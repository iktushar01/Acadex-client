const DashboardLayout = ({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) => {
    return (
        <>
            dashboard layout
            {children}
        </>
    );
};

export default DashboardLayout;